using System.Runtime.CompilerServices;

// Разбор ответа NetBIOS написан вручную и содержит нетривиальные места: перечень имён
// переменной длины и отличие собственного имени узла от группового. Ошибка здесь
// не падает, а называет все машины офиса одинаково, — такой код обязан быть покрыт
// тестами, поэтому внутренние типы открыты тестовой сборке.
[assembly: InternalsVisibleTo("StormMachine.Discovery.UnitTests")]
