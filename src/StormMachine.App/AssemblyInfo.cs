using System.Runtime.CompilerServices;

// Сборка служб и карта навигации внутренние, а проверять надо именно их: страница,
// которую контейнер не может собрать, ломается не при сборке продукта, а когда
// оператор на неё перейдёт. Разметку проверяет компилятор XAML, регистрацию — никто.
[assembly: InternalsVisibleTo("StormMachine.App.UnitTests")]

// Стенд снимков (lab/tools/PageShots) собирает те же страницы тем же контейнером
// и сохраняет их в PNG. Смотреть на показанное — единственный способ поймать то,
// что не ловят ни компилятор разметки, ни проверки: поле, схлопнутое в полоску,
// текст, уехавший за край, график, который нарисовался пустым. Сам стенд лежит
// вне git — как и вся лаборатория; продукт от его отсутствия не страдает.
[assembly: InternalsVisibleTo("PageShots")]
