using System.Runtime.CompilerServices;

// Показ — не украшение: рендереры превращают измерение в то, что оператор прочтёт
// вместо него. Ошибка здесь не роняет прогон, она сообщает неверное число, и заметить
// её нечем. До И-19 в этом проекте не было ни одного теста при девяти тысячах строк.
[assembly: InternalsVisibleTo("StormMachine.Cli.UnitTests")]
