<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Description>Платформенный слой: таймер высокого разрешения, сведения о сетевом окружении Windows и обогащение узлов маршрута.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\StormMachine.Application\StormMachine.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.11" />

    <!-- DPAPI. В базовую библиотеку .NET не входит: отдельный пакет, только Windows.
         Нужен для паролей каналов оповещения — держать их в базе открытым текстом
         нельзя, базу копируют и присылают. -->
    <PackageReference Include="System.Security.Cryptography.ProtectedData" Version="10.0.0" />
  </ItemGroup>

</Project>
