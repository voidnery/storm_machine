// Проверки показа не идут параллельно.
//
// Причина та же, по которой не идут параллельно проверки хранилища, только вместо
// пула соединений здесь Console.Out. Он один на процесс, и перехват вывода —
// Console.SetOut — глобален: два класса проверок, перехвативших вывод одновременно,
// перемешают строки и оба увидят чужие.
[assembly: CollectionBehavior(DisableTestParallelization = true)]
